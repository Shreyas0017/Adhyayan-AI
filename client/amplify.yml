version: 1
applications:
  - frontend:
      build:
        commands:
          - cd client
          - npm ci
          - npm run build
      artifacts:
        baseDirectory: client/.next
        files:
          - '**/*'
      cache:
        paths:
          - client/node_modules/**/*
